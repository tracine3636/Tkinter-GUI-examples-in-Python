import tkinter

root = tkinter.Tk()  (#) just means open window


topFrame = tkinter.Frame(root) #top frame pack in
topFrame.pack()
bottomFrame = Frame(root)
bottomFrame.pack() #bottom frame pack in
bottomFrame.pack(side=tkinter.BOTTOM)










root.mainloop() #this keeps the window open

